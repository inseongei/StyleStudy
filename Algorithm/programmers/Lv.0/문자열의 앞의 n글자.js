/* 00:55 / 10:00 */
function solution(my_string, n) {
  return my_string.substring(0, n);
}

// 문자열 my_string
// 정수 n
// my_string의 앞의 n 글자로 이루어진 문자열을 return

// 1. my_string의 0부터 n까지의 문자열을 잘라낸다. (substring)

// 사용예제1
function add(a, b) {
  return a + b;
}

const result = add(2, 3);
console.log(result);

// 사용예제2
function fullName(firstName, lastName) {
  return `${firstName} ${lastName}`;
}

const result2 = fullName("인성", "정");
console.log(result2);

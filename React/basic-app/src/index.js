import React from "react";
import ReactDOM from "react-dom/client";
import AppMentors from "./AppMentors";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<AppMentors />);
